# Archive

이 폴더는 더 이상 사용하지 않는 파일들을 보관합니다.

## 보관된 파일들

- `crawl-wanted.js` - 초기 원티드 크롤러 (현재는 `src/crawlers/wanted.js` 사용)
- `crawl-zighang.js` - 초기 직항 크롤러 (현재는 `src/crawlers/zighang.js` 사용)

이 파일들은 참고용으로만 보관되며, 프로젝트 실행에는 사용되지 않습니다.
